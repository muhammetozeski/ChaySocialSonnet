# Give Home.razor a Real Welcome Moment

Kategori: ux | Efor: S | Rank: 9

## Ne
`MainProject/UI/Pages/Home.razor` still shows the unmodified skeleton text ("Welcome to your new Hybrid App! auauauua" + "This is the empty MainProject template ready to be filled with your systems."). Replace it with a warm welcome hero card built from GlassCard/GradientCard + emoji + CoreButton. Also delete the dead classes in `HomeCss.razor` (`.home-pro-banner`, `.home-pro-banner-row`, `.home-pro-banner-text`, `.home-pro-banner-title`, `.home-pro-banner-subtitle`, `.reading-reward-row`, `.reading-reward-xp`, `.reading-reward-gold`) — verified: these are not used anywhere in the repo outside `HomeCss.razor`, and they reference a `CurrencyAmount` component that does not exist — write the hero's own classes in their place.

## Neden
The first page a user opens still shows the literal template text (typo "auauauua" included), which directly hurts the first impression and is the most visible, cheapest-to-fix UX flaw in the project. Cleaning up the dead, confusing classes in `HomeCss.razor` carries no risk since nothing else references them — verified.
